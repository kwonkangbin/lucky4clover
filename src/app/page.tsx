"use client";

import { BackIcon, CheckIcon, SearchIcon, ShareIcon } from "@/components/icon";

const Home = () => {
  return (
    <div>
      <BackIcon />
      <CheckIcon fill="#767676" />
      <SearchIcon />
      <ShareIcon />
    </div>
  );
};

export default Home;
